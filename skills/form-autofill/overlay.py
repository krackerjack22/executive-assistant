"""Spatial overlay fill for flattened (non-AcroForm) PDFs — v1.5 scaffold."""

from pathlib import Path


def fill(
    template_pdf: Path,
    profile: dict,
    index: dict,
    output_pdf: Path,
    dry_run: bool = True,
) -> dict:
    """v1.5 — spatial overlay for flattened PDFs not yet implemented."""
    raise NotImplementedError("Spatial overlay scheduled for v1.5")
